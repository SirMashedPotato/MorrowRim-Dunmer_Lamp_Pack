using Verse;

namespace Mashed_ColourableLantern
{
    public class CompProperties_GlowColourMatcher : CompProperties
    {
        public CompProperties_GlowColourMatcher()
        {
            this.compClass = typeof(Comp_GlowColourMatcher);
        }
    }
}
